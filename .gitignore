# macOS
.DS_Store
.AppleDouble
.LSOverride

# Xcode
DerivedData/
*.xcuserstate
*.xcuserdatad/
xcuserdata/
*.moved-aside
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3

# Build artifacts
build/
*.ipa
*.xcarchive

# Swift Package Manager
.build/
.swiftpm/

# Sparkle tools (downloaded in CI, not committed)
sparkle-tools/
sparkle.tar.xz

# Private keys — NEVER commit
*.pem
sparkle_private_key*

# CocoaPods (not used, but just in case)
Pods/

# Carthage (not used)
Carthage/Build/

# Personal guides (local only, not for public repo)
guide/0[0-9]-*.md
guide/README.md

# Local reference docs (vendor docs, not for public repo)
Ref
Examples

# Distributable artifacts — these belong on GitHub Releases, not in the repo
*.dmg

# Signing material — never commit certificates or private keys
*.p12
*.cer
*.mobileprovision
*.provisionprofile

# Assets tracked in assets/ — nothing here is ignored.

# Editor
.vscode/
*.swp
*~

# Build artifacts (never commit)
DoomCode.app/
DoomCode-*.zip
build/

Doom-Coder-old/

# Hook backup files created by RulesInstaller
*.doomcoder-backup-*
.github