// NSEDebugView.swift — DoomCoder Companion
//
// DEPRECATED in v3.0. The Debug tab was removed; payload diagnostics now
// live in Settings → Notification History. The file is retained as a
// no-op stub so the Xcode project keeps compiling without a `.pbxproj`
// edit. Delete on the next project regeneration pass.

import SwiftUI

struct NSEDebugView: View {
    var body: some View { EmptyView() }
}
