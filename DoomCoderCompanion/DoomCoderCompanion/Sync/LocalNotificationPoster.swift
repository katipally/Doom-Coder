// LocalNotificationPoster.swift — removed in v4.0.
// Retained as an empty file so the Xcode .pbxproj reference keeps compiling.
// The NSE is now the single source of iOS banners; local posting was removed in v3.0.
